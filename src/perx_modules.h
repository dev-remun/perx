#ifndef PERX_MODULES_H
#define PERX_MODULES_H

/**
 * a read-eval-print-loop that read an input, evaluate it,
 * then print the result of the input or the evaluation.
 * This is the core of a shell
 */
void repl();

#endif